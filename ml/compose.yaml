services:
  ml:
    build: .
    environment:
      OPENAI_API_KEY: ${OPENAI_API_KEY:-}
      LLM_MODEL: ${LLM_MODEL:-gpt-5.6-sol}
    volumes:
      - ./data:/data:ro
      - ./output:/output
    command: ["preview", "examples/packet.json", "--out", "/output/preview.json"]
