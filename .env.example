# Copy this file to .env in the repository root and save your own API key there.
# Do not commit .env. The API also works without a key when llm_enabled=false.
OPENAI_API_KEY=
LLM_MODEL=gpt-5.6-sol
API_PORT=8080
DB_PORT=5433
